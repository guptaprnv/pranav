# Core ML
torch>=2.2.0
torchvision>=0.17.0

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# Job queue & state
redis>=5.0.0

# Config
pyyaml>=6.0.1

# Monitoring (optional — enable via env var)
mlflow>=2.12.0

# Cloud backup backends (install the one you use)
boto3>=1.34.0            # AWS S3
# google-cloud-storage   # GCS
# azure-storage-blob     # Azure

# Encryption
cryptography>=42.0.0

# P2P discovery
zeroconf>=0.132.0        # mDNS / Bonjour

# System info
psutil>=5.9.0

# macOS menu bar (desktop only)
rumps>=0.4.0             # macOS only
py2app>=0.28.0           # macOS only
dmgbuild>=1.6.0          # macOS only

# Dev / testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0            # async test client
